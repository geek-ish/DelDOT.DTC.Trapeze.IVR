@model IEnumerable<MVCWebApplication2.Models.StopTime>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Direction)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Destination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StopId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeOfStop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Route)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Direction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Destination)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StopId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeOfStop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Route)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StopTimeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StopTimeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StopTimeId })
        </td>
    </tr>
}

</table>
